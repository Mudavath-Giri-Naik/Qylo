import PlaceholderPage from "@/components/PlaceholderPage";

export default function CircuitBuilderPage() {
  return (
    <PlaceholderPage
      title="Circuit Builder"
      phase="Phase 3"
      description="A drag-and-drop gate canvas and code editor, wired to a Qiskit Aer simulator, will live here."
    />
  );
}
