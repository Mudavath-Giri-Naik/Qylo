import PlaceholderPage from "@/components/PlaceholderPage";

export default function LearnPage() {
  return (
    <PlaceholderPage
      title="Learn"
      phase="Phase 2"
      description="Modules and lessons will live here: browse a module, open a lesson, and read it top to bottom."
    />
  );
}
